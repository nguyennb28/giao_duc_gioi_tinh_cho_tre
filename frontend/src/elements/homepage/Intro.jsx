import SectionImageRight from "../../components/SectionImageRight";

const IntroHomePage = ({ img, content }) => {
  return (
    <>
      <SectionImageRight image={img} content={content} />
    </>
  );
};

export default IntroHomePage;
